# Description

A simple app to test express and web application example.

# Instructions

Run `npm run watch:client` to automatically build JS file.
On other terminal, run `npm start` to initiate serve on port 3000.
